"""Application constants.

Keep lightweight, non-secret configuration values here. For now we expose
the absence threshold used to mark a student with a WARNING status in the
admin students list.
"""

# Number of absences that trigger a 'warning' status in the admin students list
WARNING_ABSENCE_THRESHOLD = 3
